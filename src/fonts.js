import { Merriweather_Sans, Twinkle_Star } from "next/font/google";

const merriweatherSans = Merriweather_Sans({ subsets: ['latin-ext'] });
const twinkleStar = Twinkle_Star({ subsets: ['latin'], weight: '400' });

export { merriweatherSans, twinkleStar };
