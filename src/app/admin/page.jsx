import PhotoManager from './PhotoManager';

export const metadata = { title: 'Photo manager | Life of Jupiter', robots: { index: false, follow: false } };
export default function AdminPage() { return <PhotoManager />; }
